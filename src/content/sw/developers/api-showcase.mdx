---
title: Pakia media & showcase
description: Rejea ya /api/upload na /api/showcase — selfie, hati (KYC) na miradi ya showcase.
group: api
order: 13
updated: 2026-09-19
---

Uploads hudhibitiwa na `multer` (in-memory, kikomo cha faili **5 MB**) na kupelekwa kwenye Cloudinary. Endpoint zote zinahitaji **JWT**.

## Upload / KYC (`/api/upload`)

| Method | Path | Auth | Maelezo |
|--------|------|------|-------------|
| POST | `/upload/selfie` | JWT | Pakia picha ya selfie — multipart, field `file` |
| POST | `/upload/document` | JWT | Pakia hati (KYC) — multipart, field `file` |

Zote hupokea faili moja kwa wakati mmoja, `image/*` au `application/pdf`, hadi **5 MB**. Zaidi ya faili moja au faili kubwa ~`400`.

<Callout type="note">
Kwa uthibitisho wa KYC (selfie, hati, hati ya biashara) kila faili huingizwa moja kwa moja. Pakia selfie kwanza, kisha hati.
</Callout>

Majibu yanarudisha URL ya Cloudinary na ujumbe:

```json
{ "url": "https://res.cloudinary.com/…/selfie.jpg", "message": "Selfie uploaded" }
```

## Showcase (`/api/showcase`)

Miradi ya **showcase** ni kazi za kuonyesha zinazopatikana kwenye ukurasa wa "showcase" / "explore".

| Method | Path | Auth | Maelezo |
|--------|------|------|-------------|
| GET | `/showcase` | — | Orodhesha miradi ya showcase |
| POST | `/showcase/create` | `superAdmin` | Unda mradi (multipart, field `image`) |
| PUT | `/showcase/:id` | `superAdmin` | Sasisha mradi |
| DELETE | `/showcase/:id` | `superAdmin` | Futa mradi |
